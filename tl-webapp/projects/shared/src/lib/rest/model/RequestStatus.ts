export enum RequestStatus {
        NOT_REQUESTED = "NOT_REQUESTED",
        REQUESTED = "REQUESTED",
        ACCEPTED = "ACCEPTED",
        REJECTED = "REJECTED",
        WAITLISTED = "WAITLISTED"
    }
